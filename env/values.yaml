jenkins-x-platform:
  PipelineSecrets:
    DockerConfig: |-
      {
        "credsStore": "ecr-login"
      }
  docker-registry:
    enabled: false
  nexus:
    enabled: false
  postinstalljob:
    enabled: "true"
